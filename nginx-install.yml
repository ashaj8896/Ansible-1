pipeline {
    agent any
    stages {
        stage('Ansible Playbook') {
            steps {
                ansiblePlaybook(
                    playbook: 'nginx-install.yml',
                    
                )
            }
        }
    }
}
